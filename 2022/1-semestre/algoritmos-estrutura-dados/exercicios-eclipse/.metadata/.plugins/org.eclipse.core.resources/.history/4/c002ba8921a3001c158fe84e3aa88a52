package principal;

public class ListaEstatica 
{
	private int[] info;
	private int tamanho;
	
	public ListaEstatica() 
	{
		info = new int[10];
		tamanho = 0;
	}	
	
	private void redimensionar()
	{
		// declara novo vetor
		int[] novo;
		// declara o novo tamanho
		int novoTamanho = info.length + 10;
		
		// determina o tamanho do novo vetor
		novo = new int[novoTamanho];
		
		// processo de cópia de um vetor para outro
		for (int i = 0; i < tamanho; i++) 
		{
			novo[i] = info[i];
		}
		
		info = novo;
	}
	
	public void inserir(int valor)
	{
		if (tamanho == info.length)
		{
			this.redimensionar();
		}
		
		this.info[tamanho] = valor;
		tamanho++;
	}
	
	public String exibir()
	{
		String retorno = "[";
		for (int i = 0; i < tamanho; i++)
		{
			retorno += info[i] + ", ";
		}
		return retorno + "]";
	}
	
	public int buscar(int valor)
	{
		// percorrer somente as posições usadas
		for (int posicao = 0; posicao < tamanho; posicao++)
		{
			if (info[posicao] == valor)
			{
				return posicao;
			}
		}
		return -1;
	}
	
	public void retirar(int valor)
	{
		// cria array temporário com o tamanho do array original -1 (deletar)
		int[] arrayTemp = new int[info.length - 1];
		
		for (int i = 0; i < tamanho; i++)
		{
			if (info[i] == valor)
			{
				continue;		
			}
			
			arrayTemp[i] = info[i];
		}
	}
	
	public boolean estaVazia()
	{
		// para estar vazio, o array deve ter tamanho 0
		if (tamanho == 0)
		{
			return true;
		}
		
		return false;
	}
}
